arr = [3, 5, 1, 9, 2]

min_val = arr[0]
max_val = arr[0]

for i in arr:
    if i < min_val:
        min_val = i
    if i > max_val:
        max_val = i

print("Minimum element:", min_val)
print("Maximum element:", max_val)
